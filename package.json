{
  "name": "samo-crm",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:backend": "npm --workspace backend run dev",
    "build": "npm --workspace frontend run build && npm --workspace backend run build",
    "test": "npm --workspace frontend test && npm --workspace backend test",
    "typecheck": "npm --workspace frontend run build && npm --workspace backend run typecheck"
  }
}
